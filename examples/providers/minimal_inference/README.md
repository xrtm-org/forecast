# Minimal Inference

**Script**: `run_minimal_inference.py`

A bare-bones example of checking model connectivity. Use this script as a "health check" to verify your API keys and network connection to the LLM provider are working correctly.

## Usage

```bash
# From repository root
python3 examples/providers/minimal_inference/run_minimal_inference.py
```
